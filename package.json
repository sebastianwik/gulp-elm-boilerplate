{
  "name": "gulp-elm-boilerplate",
  "version": "1.0.0",
  "description": "A simple boilerplate for Elm, Gulp and Browser-sync",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "elm package install",
    "start": "gulp serve"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.11.1",
    "del": "^2.2.0",
    "gulp": "^3.9.0",
    "gulp-elm": "^0.2.0"
  }
}
